version: "3.8"

services:
  ros:
    container_name: robot-arm-container
    build:
      context: .
    image: robot-arm-ros
    stdin_open: true
    tty: true
    volumes:
      - ./ros2_ws:/home/dockeruser/ros2_ws
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
